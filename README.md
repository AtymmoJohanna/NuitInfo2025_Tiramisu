## 🧩 Technologies utilisées

- React(Create React App)
- Tailwind CSS (styling)
- Lucide React (icônes)
- TensorFlow.js (inférence du modèle IA dans le navigateur)
- COCO-SSD (modèle de détection d’objets)
- @tensorflow/tfjs-backend-webgl pour de meilleures performances


### Installer les dépendances principales :

pip install npm

npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p

npm install lucide-react
npm install @tensorflow/tfjs @tensorflow-models/coco-ssd


### Lancer l’application en développement :

npm start (puis ouvrir en general http://localhost:3000)









